# EDS2_Project

## About Me
นางสาวสุพิชญา คงโพธิ์น้อย รหัสนักศึกษา 683020597-1

"This sentence is written on Hub"
 
 ---
 ## Create python environment
 `python3 -m venv .venv` - Create the new python environment named `.venv` .

The first python script file:
``` python
import pandas
print('Hello World1!')
print('This is a sample Python application.')
df = pandas.DataFrame({'A': [1, 2, 3], 'B': [4, 5, 6]})
print('My first DataFrame')
print(df)
```

## About Streamlit web application
- Install streamlit web application package `pip install streamlit`
- Run streamlit script file `steamlit run <app-name.py>`
- Stop web application `Ctl c`